# minikv config — benchmarking variant for the docker-compose stack.
#
# Only the keys that differ from the in-image default are listed; everything
# else falls through to the binary's compiled-in defaults.

pprof-addr       = 0.0.0.0:6060
max-memory-bytes = 67108864
